packages:
  - "apps/*"
  - "packages/*"

# Dependências autorizadas a rodar build scripts (binários nativos).
# esbuild/@swc/core: vite/ladle · sharp: otimização de imagem do Next · msw: worker (ladle).
onlyBuiltDependencies:
  - esbuild
  - "@swc/core"
  - sharp
  - msw
