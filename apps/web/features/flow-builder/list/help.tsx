export function FlowsHelp() {
  return (
    <div className="space-y-2 text-sm text-text-low">
      <p>
        Flows automatizam acoes a partir de gatilhos (mensagem, palavra-chave, resposta de
        formulario). Cada flow e um grafo visual de nos deterministicos — sem IA no loop.
      </p>
      <p>
        Crie um rascunho, desenhe o grafo no editor e publique para ativar. Flows manuais aparecem
        na barra de acoes da conversa; arraste para reordenar.
      </p>
    </div>
  );
}
