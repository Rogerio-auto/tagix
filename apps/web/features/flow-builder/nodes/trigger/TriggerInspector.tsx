'use client';

export function TriggerInspector(_props: { nodeId: string }) {
  return (
    <div className="flex flex-col gap-3">
      <p className="text-xs text-text-low">
        O no inicial define o ponto de entrada do flow e nao tem configuracao.
      </p>
    </div>
  );
}
