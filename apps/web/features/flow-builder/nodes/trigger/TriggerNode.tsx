'use client';

// Stub do node 'trigger' (F4-S10 scaffold). S11 substitui por UI rica.
// O canvas registra via nodeTypes; ate la usa o BaseNode do catalogo.
import { makeNodeComponent } from '../BaseNode';

export const TriggerNode = makeNodeComponent('trigger');
