'use client';

// Node 'register_conversion' (F31-S08 espinha). Usa o BaseNode do catalogo.
import { makeNodeComponent } from '../BaseNode';

export const RegisterConversionNode = makeNodeComponent('register_conversion');
