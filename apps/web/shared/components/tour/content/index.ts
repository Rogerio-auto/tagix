/**
 * Conteúdo dos tours guiados (F43-S08). Barrel da pasta `content`.
 *
 * `APP_TOURS` é registrado no `TourProvider` pelo `GuidedTourMount`; o
 * `TOUR_BY_PATHNAME` dirige o auto-start por rota na primeira visita do membro.
 */
export { APP_TOURS, TOUR_BY_PATHNAME } from './tours';
