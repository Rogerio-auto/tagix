import { AvailabilityRulesSettings } from '@/features/calendar/availability';

export const metadata = { title: 'Disponibilidade' };

export default function Page(): React.JSX.Element {
  return <AvailabilityRulesSettings />;
}
