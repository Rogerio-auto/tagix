import { PolicyEditor } from '@/features/platform-admin/policies';

export const metadata = { title: 'Plataforma — Politicas' };

export default function PlatformPoliciesPage() {
  return <PolicyEditor />;
}
