import { ModelsCatalog } from '@/features/platform-admin/models';

export const metadata = { title: 'Plataforma — Modelos' };

export default function PlatformModelsPage() {
  return <ModelsCatalog />;
}
