SLOT F31-S04 — Inspector interactive completo (botoes reply/url/phone + listas). Especialista: frontend-engineer.

files_allowed:
- apps/web/features/flow-builder/nodes/interactive/**  (InteractiveInspector.tsx, InteractiveNode.tsx, metadata.ts)
- packages/flow-engine/src/handlers/interactive.handler.ts

OBJETIVO: editor ESTRUTURADO (nao JSON cru) para interactive. Hoje so edita kind+body. Adicionar:
- BOTOES: tipos reply (id+title) / URL (title+url) / PHONE (title+phoneNumber). Editor de lista com add/remove/reorder.
- LISTAS: sections (title + rows[{id,title,description?}]) com buttonLabel.
- header/footer (texto). body interpolavel (VariablesPicker).
- Validacao de LIMITES do provider sinalizada inline (WhatsApp: max 3 reply buttons; list: max 10 rows por section / limites de sections; titulos com limite de chars). Use Zod no submit + estados de erro DS v2.
- Preview do interactive (WYSIWYG: botoes/lista renderizados).

HANDLER (interactive.handler.ts): hoje schema = { kind:'buttons'|'list', body, header?, footer?, buttons?[{id,title}], buttonLabel?, sections?[{title?,rows[{id,title,description?}]}] }. EXPANDA buttonSchema para suportar tipo (reply|url|phone) com campos url?/phoneNumber? (discriminado ou union). Repasse o payload rico em ctx.sendMessage.interactivePayload. NOTA: o publisher do S01 ainda trata interactivePayload como no-op conservador (precisa traducao p/ InteractivePayloadSchema do OutboundJob, um follow-up do bridge fora deste slot) — entao o ENVIO e2e de interactive pode nao chegar ao provider ainda; faca o shape/handler corretos e RELATE essa dependencia. Foco do slot: UI rica + handler shape + validacao.

USE os pickers/VariablesPicker do S03 onde fizer sentido (body interpolavel). NAO toque helpers-context/VariablesPicker/InspectorPanel/registry/node-catalog.

DoD: cria botoes reply/url/phone e listas; limites validados inline; preview. typecheck/lint/test verdes.
Validacao: pnpm --filter @hm/web typecheck; pnpm --filter @hm/flow-engine typecheck; pnpm --filter @hm/flow-engine test; eslint nos arquivos.
