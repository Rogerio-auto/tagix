SLOT F31-S08 — Scaffold dos novos nodes + limpeza do catalogo (ESPINHA). Especialista: frontend-engineer (toca flow-engine + web).

files_allowed:
- packages/flow-engine/src/registry.ts
- packages/flow-engine/src/validation.ts
- packages/flow-engine/src/handlers/{set_variable,input,assign,template,ab_split,go_to_flow}.handler.ts  (6 NOVOS handlers)
- apps/web/features/flow-builder/shared/node-catalog.ts
- apps/web/features/flow-builder/nodes/nodeTypes.ts
- apps/web/features/flow-builder/nodes/nodeInspectors.ts
- apps/web/features/flow-builder/nodes/{set_variable,input,assign,template,ab_split,go_to_flow,register_conversion}/**  (7 dirs NOVOS de UI)

OBJETIVO: criar a ESPINHA dos 7 novos nodes (6 kinds novos + register_conversion que ja tem handler mas faltava node na UI) e limpar o catalogo. S09/S10/S11 vao PREENCHER os stubs dos seus nodes depois — por isso eles dependem deste slot e NUNCA rodam em paralelo com voce. Voce cria STUBS funcionais minimos; eles sobrescrevem.

ESTADO ATUAL:
- registry.ts: 16 handlers (inclui register_conversion). FlowNodeType = keyof handlerRegistry (exaustivo via const). FLOW_NODE_TYPES derivado.
- node-catalog.ts: FlowNodeKind = union de 15 kinds (SEM register_conversion). NODE_CATALOG: Record<FlowNodeKind,NodeMeta>. add_tag/remove_tag/move_stage tem `deferred:true` (STALE — handlers sao reais desde a F5: REMOVA esses 3 flags).
- nodeTypes.ts / nodeInspectors.ts: Record exaustivo dos 15 kinds -> componente.

ESCOPO:
1) node-catalog.ts: adicionar 7 kinds a FlowNodeKind e a NODE_CATALOG: register_conversion (label "Registrar conversao", category 'system'), set_variable ("Definir variavel", system), input ("Capturar resposta", timing ou logic — escolha coerente; edges p/ resposta/timeout se aplicavel), assign ("Atribuir conversa", system), template ("Template/HSM", output), ab_split ("Teste A/B", logic, edges por variante ex.: ['a','b'] ou por peso), go_to_flow ("Ir para flow", logic ou system). Escolha icons lucide coerentes. Defina edges certas (ab_split tem multiplas; input pode ter response/timeout; demais default). REMOVA `deferred:true` de add_tag/remove_tag/move_stage.
2) registry.ts: importar e registrar os 6 NOVOS handlers (set_variable/input/assign/template/ab_split/go_to_flow). register_conversion JA esta registrado. Atualize os comentarios "15/16" p/ o numero real (agora 22). Mantenha o satisfies/Record check exaustivo.
3) validation.ts: se necessario, adicionar novos var roots (ex.: variaveis de set_variable/input) ao KNOWN_VAR_ROOTS, e cobrir os novos shapes. NAO quebre as regras existentes (1 trigger, alcancabilidade, ciclos). Para ab_split/input com edges nomeadas, garanta que a validacao de edges aceite os novos sourceHandles.
4) handlers/*.handler.ts (6 novos): STUBS funcionais minimos { schema: zod minimo, async execute(node, ctx){ return { status:'SUCCESS' } } } — TIPADOS, zero any, compilando. S09/S10/S11 implementam de verdade. (set_variable, input -> S09; assign, template -> S10; ab_split, go_to_flow -> S11.) Faca o schema minimo plausivel para o stub nao quebrar (campos opcionais).
5) nodes/<tipo>/ (7 dirs): cada um com <Tipo>Node.tsx (node visual minimo, pode reusar BaseNode) + <Tipo>Inspector.tsx (inspector stub minimo com 'use client' + useFlowEditor) + metadata.ts se o padrao exigir. STUBS — S09/S10/S11 preenchem. Exporte os nomes que nodeTypes.ts/nodeInspectors.ts importam (ex.: SetVariableNode/SetVariableInspector).
6) nodeTypes.ts + nodeInspectors.ts: adicionar as 7 entradas (o Record<FlowNodeKind,...> agora exige todas — exaustivo).

CRITICO: ao terminar, o `pnpm typecheck` deve ficar VERDE nos 13 projetos (Record exaustivo + FlowNodeType/FlowNodeKind consistentes). Stubs minimos mas type-safe.

PROIBIDO: handlers/message.handler.ts e handlers de nodes existentes; handlers/register_conversion.handler.ts (ja existe — nao reescreva); helpers-context.tsx; VariablesPicker.tsx; inspector/pickers.

DoD: novos kinds no palette/catalogo; register_conversion vira o 16o node na UI; flags deferred removidas; stubs mantem typecheck verde; lint/test verdes.
Validacao: pnpm typecheck (13 projetos); pnpm --filter @hm/flow-engine test; eslint nos arquivos tocados.
