SLOT F31-S05 — Inspector http_request completo (headers/body/retry/map-resposta). Especialista: frontend-engineer.

files_allowed:
- apps/web/features/flow-builder/nodes/http_request/**  (HttpRequestInspector.tsx, HttpRequestNode.tsx, metadata.ts)
- packages/flow-engine/src/handlers/http_request.handler.ts

OBJETIVO: hoje so method+url. Adicionar:
- HEADERS: editor key/value estruturado (add/remove). BODY: editor (json/raw) com interpolacao de variaveis (VariablesPicker).
- RETRY: tentativas + backoff configuravel.
- MAPEAMENTO DA RESPOSTA: JSONPath -> variavel (ex.: $.data.id -> {{webhook_response.id}} ou variavel custom). Editor de mapeamentos com preview/validacao do JSONPath. A variavel mapeada deve ser usavel a jusante.
- Edges success/error ja existem (roteiam por status/erro) — preserve.
- Seguranca: o handler http JA deve ter allowlist/timeout anti-SSRF — PRESERVE; nao exponha credenciais/headers sensiveis em log. Nao quebre o comportamento de seguranca existente.

HANDLER (http_request.handler.ts): expanda o schema p/ headers/body/retry/responseMapping conforme a UI passar a mandar. Implemente o mapeamento JSONPath -> ctx.variables (grava no contexto). Mantenha allowlist/timeout. Use unknown+Zod, zero any.

USE VariablesPicker do S03 no body/headers. NAO toque helpers-context/VariablesPicker/InspectorPanel/registry/node-catalog.

DoD: headers/body/retry configuraveis; resposta mapeada p/ variavel usavel; edges success/error. typecheck/lint/test verdes.
Validacao: pnpm --filter @hm/web typecheck; pnpm --filter @hm/flow-engine typecheck; pnpm --filter @hm/flow-engine test; eslint nos arquivos.
