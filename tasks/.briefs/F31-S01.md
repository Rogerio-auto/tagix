Voce e o backend-engineer executando o slot F31-S01 do tagix (Flow Builder v2). Padrao world-class, TS strict, ZERO `any` (use unknown+Zod).

REGRAS DE EXECUCAO (harness sem worktree):
- Escreva APENAS nos arquivos do files_allowed abaixo. NAO rode nenhum comando git. NAO rode pnpm install.
- Ao terminar, rode os comandos de validacao e DEIXE a arvore com os arquivos escritos (eu, o orchestrator, integro via git).
- NAO toque em nenhum outro arquivo. Se precisar de algo fora do files_allowed, PARE e relate no final em vez de editar.

FILES_ALLOWED (fronteira sagrada):
- packages/flow-engine/src/types.ts        (editar: expandir FlowOutboundMessage + FlowPresenceAction)
- packages/flow-engine/src/ports/outbound.port.ts  (editar: doc; contrato estavel)
- apps/workers/src/flows/outbound-publisher.ts      (NOVO: OutboundPublisher real)
- apps/workers/src/flows/worker.ts                  (editar: injetar o publisher real em createFlowWorkerDeps)
- apps/workers/src/flows/outbound-publisher.test.ts (NOVO: Vitest)

OBJETIVO: Fazer um flow ENVIAR MENSAGEM DE VERDADE em producao. Hoje createFlowWorkerDeps nao injeta publisher -> outbound cai no noopPublisher -> ctx.sendMessage/sendPresence sao no-op. Substituir por um OutboundPublisher real que (1) persiste a message em estado pending sob RLS, (2) resolve midia via storage.getSignedUrl(key, ttl) -> publicMediaUrl, (3) faz publishOutboundJob no shape EXATO de parseOutboundJob.

CONTRATO RICO de FlowOutboundMessage (expandir em packages/flow-engine/src/types.ts; hoje tem text/mediaStorageKey/mediaType/audioMessageKind('voice'|'audio_file')/interactivePayload). Garanta cobrir: texto; imagem/video/documento com mediaStorageKey + mediaType(mime) + caption; audio com audioMessageKind ('voice' = nota de voz | 'audio_file' = arquivo encaminhado). Mantenha FlowPresenceAction (presence typing/recording + durationMs). NAO quebre os consumidores existentes (handlers message/interactive/external_notify); so amplie campos opcionais.

ESPELHE EXATAMENTE o composer da API (NAO reinventar o job):
- apps/api/src/mq/outbound-publisher.ts -> publishOutboundJob(workspaceId, job): makeEnvelope('outbound.job',...) + publish(channel, `${QUEUES.outbound}.send`, envelope). Reuse esse mesmo padrao (connectMq lazy singleton por processo). Voce pode importar de @hm/shared/mq.
- apps/api/src/routes/conversations/messages.ts:104-138 buildOutboundJob -> shape do job. base = { channelId: conv.channelId, conversationId, messageId, chatId: conv.remoteId }. kind 'text' = { ...base, text }. kind 'media' = { ...base, mediaKind, publicMediaUrl, mime, caption? }. (template/interactive: ver outboundJobSchema.)
- apps/workers/src/outbound/job.ts -> parseOutboundJob / outboundJobSchema (discriminatedUnion 'kind': text|media|template|interactive|ig_*|typing_indicator). publicMediaUrl deve ser z.string().url(). mediaKind in image|video|audio|voice|document|sticker. NAO importe esse modulo cruzando boundary se causar ciclo — replique o shape como Record<string,unknown> e deixe o worker outbound validar (mesma estrategia da API, que monta unknown de proposito).
- Persistencia da message pending (espelhe messages.ts:242-269): select conversations { channelId, remoteId } por conversationId sob RLS (use withWorkspace de @hm/db); insert schema.messages { workspaceId, conversationId, direction:'outbound', senderType: <apropriado p/ sistema/flow — verifique o enum; provavelmente 'system' ou 'bot'; cheque schema>, type, content, viewStatus:'pending', mediaUrl, mediaMime, mediaCaption }.returning(). Use o messageId retornado no job.

MIDIA: FlowOutboundMessage guarda mediaStorageKey (key no storage). No envio, resolva URL publica temporaria via storage.getSignedUrl(key, ttl). Importe createStorage de @hm/storage (ver exemplos: apps/workers/src/media/worker.ts:23,61 e apps/workers/src/privacy/index.ts:11,34). Injete o storage no construtor do publisher (default createStorage()) para testabilidade.

AUDIO voice-vs-arquivo: mapeie audioMessageKind 'voice' -> mediaKind 'voice'; 'audio_file' -> mediaKind 'audio'. (outboundMediaKindSchema tem ambos.)

CAPABILITY-AWARE (presence + degradacao): publishPresence emite typing_indicator job (ver outboundJobSchema kind 'typing_indicator': precisa chatId + targetExternalId + presence). Se o canal nao suportar presenca, no-op silencioso. Procure AdapterCapabilities (grep no repo: provavelmente em @hm/channels). Se resolver capability exigir tocar arquivo fora do files_allowed, faca degradacao conservadora (tente publicar; deixe o worker outbound/adapter ignorar) e RELATE a limitacao no final. NAO edite fora do files_allowed.

worker.ts: em createFlowWorkerDeps, construa o OutboundPublisher real e passe-o ao createOutboundPort do flow-engine. Hoje createFlowWorkerDeps so monta queue+engine. Veja como o flow-engine recebe o outbound port: cheque packages/flow-engine/src/index.ts (createFlowEngine) e packages/flow-engine/src/deps.ts para a assinatura de injecao do outbound port. Se createFlowEngine ainda nao aceitar outbound port injetado, mas voce SO pode editar worker.ts + outbound.port.ts (nao index.ts): use a fabrica createOutboundPort(publisher) ja exportada de outbound.port.ts e passe via opcoes de createFlowEngine se houver; se a unica forma de injetar for editar index.ts (fora do files_allowed), PARE e relate o seam exato necessario. (Provavelmente createFlowEngine ja aceita { queue, outbound } — confirme.)

outbound.port.ts: so atualize o doc (nao e mais no-op em prod) e, se necessario, ajuste a assinatura de createOutboundPort/OutboundPublisher para o contrato rico. Mantenha o noopPublisher como default para testes/engine pura.

TESTE (outbound-publisher.test.ts, Vitest): com fakes (storage fake getSignedUrl, publish fake, withWorkspace mockado ou DB de teste): (1) message texto -> job kind text com shape correto; (2) message imagem -> resolve signed url -> job kind media publicMediaUrl/mime/caption; (3) audio voice -> mediaKind voice; audio_file -> mediaKind audio; (4) presence typing -> typing_indicator (ou no-op se incapaz). Espelhe o estilo dos testes existentes (apps/workers/src/privacy/privacy.test.ts mostra fakes de storage).

VALIDACAO (rode antes de terminar):
  pnpm --filter @hm/flow-engine typecheck   (ou pnpm typecheck se preferir)
  pnpm --filter @hm/workers typecheck
  pnpm --filter @hm/workers lint
  pnpm --filter @hm/workers test
  pnpm --filter @hm/flow-engine test

Ao final, reporte: arquivos escritos, resultado da validacao, e QUALQUER seam que precisou (ou precisaria) tocar fora do files_allowed — especialmente a assinatura de injecao do outbound port em createFlowEngine. NAO escreva relatorio em .md; responda no output.

SEAM CONFIRMADO (nao precisa tocar index.ts): createFlowEngine(overrides: Partial<FlowEngineDeps>) ja aceita `outbound`. createOutboundPort(publisher) e OutboundPublisher JA sao exportados de @hm/flow-engine (index.ts:92). Logo em worker.ts faca: const engine = createFlowEngine({ queue, outbound: createOutboundPort(realPublisher) }). Tudo dentro do files_allowed.
