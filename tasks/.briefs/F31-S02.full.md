=== CONTRATO COMUM F31 Onda B (todos os slots de inspector/node) ===
Voce e engineer do tagix Flow Builder v2. Padrao world-class, DS v2 dark-first (tokens semanticos de @hm/design-tokens; ZERO hex em JSX), TS strict, ZERO `any` (unknown+Zod). Aplica UX_PRINCIPLES (docs/): pickers pesquisaveis no lugar de id-em-texto-cru; preview/WYSIWYG; estados de erro DS v2; validacao no submit.

REGRAS DE EXECUCAO (harness sem worktree):
- Escreva APENAS nos arquivos do files_allowed do SEU slot. NAO rode git. NAO rode pnpm install.
- Validacao: tente rodar; se o harness bloquear pnpm/tsc, faca revisao manual rigorosa e relate (o orchestrator roda a validacao na integracao).
- Se precisar tocar algo fora do files_allowed, PARE e relate o seam. NAO edite fora.
- NAO escreva relatorio .md; responda no output: arquivos escritos, validacao, seams.

PADROES DO REPO (ja existem no main):
- Inspector pattern: 'use client'; useFlowEditor((s)=>s.nodes) + s.updateNodeData(nodeId, patch); node = nodes.find(n=>n.id===nodeId). data = (node.data ?? {}) as Record<string,unknown>. set = (patch)=>update(nodeId, patch).
- Campos: import { Field, TextField, TextAreaField, SelectField } from '../inspector-fields' (DS v2 prontos).
- PICKERS (do F31-S03, ja em main): import { AgentPicker, ChannelPicker, TagPicker, StagePicker, PipelinePicker, ConversionTypePicker, MemberPicker, MetaFlowPicker, CustomFieldPicker, Combobox } from '@/features/flow-builder/inspector/pickers'. PickerProps: { value: string|undefined; onChange:(v:string)=>void; label?; hint?; placeholder?; disabled? }. StagePicker/CustomFieldPicker aceitam pipelineId? para filtrar. CustomFieldPicker value = custom field KEY. MetaFlowPicker aceita valor livre. Dados vem de useFlowHelpers() (provider FlowHelpersAutoProvider). USE OS PICKERS no lugar de id cru.
- VariablesPicker: import { VariablesPicker } from '../../inspector/VariablesPicker'; <VariablesPicker onPick={(token)=>...} /> insere {{...}}.
- Handlers (packages/flow-engine/src/handlers/*.handler.ts): { schema: zod, async execute(node, ctx) }. ctx.sendMessage(FlowOutboundMessage)/ctx.sendPresence/ctx.setConversationAi/ctx.setConversationStatus + ctx.variables. interpolate(text, ctx.variables) de '../utils/interpolate'. Retorno { status:'SUCCESS'|'ERROR'|..., error? }.
- FlowOutboundMessage (F31-S01, ja em main): { conversationId; text?; mediaStorageKey?; mediaType?(mime); mediaKind?(image|video|audio|voice|document|sticker); caption?; audioMessageKind?('voice'|'audio_file'); interactivePayload? }. ctx.sendMessage agora ENVIA DE VERDADE em prod (texto/midia funcionam; interactivePayload ainda e no-op conservador no publisher — ver follow-up S01: precisa traducao p/ InteractivePayloadSchema do job).
SLOT F31-S02 — Node de mensagem rico (texto / midia / voz / audio-arquivo). Especialista: frontend-engineer.

files_allowed:
- apps/web/features/flow-builder/nodes/message/**  (MessageInspector.tsx, MessageNode.tsx, metadata.ts)
- packages/flow-engine/src/handlers/message.handler.ts

OBJETIVO: o node `message` num unico node com SELETOR DE TIPO suporta: Texto; Imagem+legenda; Video+legenda; Documento/anexo; Audio como NOTA DE VOZ (audioMessageKind='voice') vs Audio como ARQUIVO encaminhado (audioMessageKind='audio_file'). Interpolacao de variaveis no texto/legenda (use VariablesPicker). Capability-aware (esconde o que o canal nao envia, se houver sinal de canal; senao mostre todos com hint).

ESTADO ATUAL (MessageInspector.tsx): so TextArea(texto) + TextField(midia storage key cru) + Select(preAction). RUIM: midia e key colada na mao. MELHORE: seletor de tipo (texto/imagem/video/documento/audio), e por tipo os campos certos. Mantenha preAction (typing/recording) + preActionDurationMs.

UPLOAD DE MIDIA: reuse o pipeline de signed-url do web. Referencia: apps/web/features/pipeline/deal/queries.ts (useRequestSignedUrl -> POST /api/deals/:id/attachments/signed-url -> PUT direto no storage -> persist metadata) e apps/web/features/pipeline/deal/CardImageCapture.tsx. POREM esse endpoint e deal-scoped. Se NAO existir endpoint generico de upload de midia de flow, implemente o upload reusando o que houver e GUARDE o `key` retornado em node.data (campo mediaUrl ja e tratado como storage key pelo handler -> mantenha esse contrato, ou renomeie para mediaStorageKey de forma consistente entre inspector e handler). Se faltar endpoint backend (fora do files_allowed), faca o melhor possivel (ex.: campo de key + botao de upload que usa endpoint existente) e RELATE o seam. Mostre preview da bolha (WYSIWYG) com o tipo selecionado. Estados de erro DS v2 (upload falho / mime invalido).

HANDLER (message.handler.ts): hoje schema = { text?, mediaUrl?, mediaType?, preAction?, preActionDurationMs?, audioMessageKind?('voice'|'audio_file') }. Execute ja faz ctx.sendMessage({ text: interpolate(...), mediaStorageKey: data.mediaUrl, mediaType, audioMessageKind }). EXPANDA o schema/execute se o inspector passar a mandar mais (ex.: caption separada do texto p/ midia; mediaKind explicito image|video|document; voz vs arquivo). Passe `caption` e `mediaKind` ao ctx.sendMessage quando aplicavel (FlowOutboundMessage suporta caption/mediaKind). Mantenha retrocompat: campos opcionais. NAO toque types.ts/registry/node-catalog.

DoD: envia texto, imagem+legenda, video, voz e audio-arquivo via WAHA dev (tipo correto no provider — agora REAL gracas ao S01); upload persiste key; preview na bolha. typecheck/lint verdes.

Validacao: pnpm --filter @hm/web typecheck; pnpm --filter @hm/flow-engine typecheck; pnpm --filter @hm/flow-engine test; eslint nos arquivos tocados.
