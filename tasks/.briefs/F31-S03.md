Voce e o frontend-engineer executando o slot F31-S03 do tagix (Flow Builder v2). Padrao world-class, DS v2 dark-first, TS strict, ZERO `any`. Aplica UX_PRINCIPLES (docs/): picker pesquisavel no lugar de id-em-texto-cru; variaveis agrupadas e legiveis (nao chaves opacas).

REGRAS DE EXECUCAO (harness sem worktree):
- Escreva APENAS nos arquivos do files_allowed abaixo. NAO rode git. NAO rode pnpm install.
- Ao terminar, rode a validacao e deixe os arquivos escritos; eu (orchestrator) integro via git.
- Se precisar tocar algo fora do files_allowed (ex.: montar o provider em FlowEditorPage.tsx, dono de outro slot), PARE e relate o seam no final. NAO edite fora.

FILES_ALLOWED (fronteira sagrada):
- apps/web/features/flow-builder/shared/helpers-context.tsx  (editar)
- apps/web/features/flow-builder/inspector/VariablesPicker.tsx  (editar)
- apps/web/features/flow-builder/inspector/pickers/**          (NOVO dir: pickers reutilizaveis)

OBJETIVO: dar a TODOS os inspectors (S04-S08) a base para escolher recursos por PICKER (nao id cru) e referenciar VARIAVEIS completas. Slot de infra compartilhada — destrava a Onda 2 e os novos nodes.

ESTADO ATUAL:
- helpers-context.tsx: FlowHelpers expoe so { agents, channels, tags, stages } (arrays {id,name}), via context com value prop. FlowHelpersProvider/useFlowHelpers existem mas NAO sao consumidos/montados em lugar nenhum ainda. Voce e quem define a base.
- VariablesPicker.tsx: 7 vars hardcoded (KNOWN_VARS), render flat de pills.

ESCOPO:
1) helpers-context.tsx — expandir FlowHelpers para os 5 NOVOS dominios alem dos 4 atuais:
   pipelines, conversion-types (tipos de conversao), meta-flows, custom-fields, members.
   Cada um como array {id; name; ...campos uteis (ex: pipeline.stages, customField.type)}.
   PADRAO: NAO duplicar fetch. Reuse os query hooks de dados ja existentes do web. Fontes confirmadas no repo:
     - pipelines: apps/web/features/pipeline/board/queries.ts
     - conversion-types: apps/web/features/conversions/queries.ts
     - custom-fields: apps/web/features/pipeline/custom-fields/ (schema.ts) — ache o hook de listagem
     - members/agents/teams: apps/web/features/settings/sections/workspace-org/queries.ts (useTeams/useDepartments) + agents em apps/web/features/agents
     - tags: apps/web/features/settings/sections/workspace-data/queries.ts (TagsManager usa)
     - meta-flows: apps/web/features/flow-builder/nodes/meta_flow/metadata.ts ja referencia; ache o hook/fonte
   DESIGN: torne o FlowHelpersProvider SELF-FETCHING (um provider client-side que chama os hooks acima e popula o context), OU mantenha a value-prop mas adicione um provider de conveniencia que faz o fetch. O objetivo e que S04-S08 so chamem useFlowHelpers() e recebam tudo populado. Use @tanstack/react-query como o resto do web (cheque o padrao dos queries.ts existentes). Trate loading/empty graciosamente (arrays vazios como fallback). Se montar o provider exigir editar FlowEditorPage.tsx (fora do escopo), exporte o provider pronto e RELATE que o mount precisa acontecer la (seam para S08/integracao).

2) VariablesPicker.tsx — catalogo de variaveis COMPLETO, agrupado e pesquisavel (nao 7 pills cruas):
   Grupos: Contato (contact.name/phone/email/...campos custom se aplicavel), Conversa (conversation.status/...), Trigger (trigger.message/...), Respostas de nodes anteriores (last_response, webhook_response.body/.status/.headers), Variaveis de set_variable (dinamicas — placeholder/lista das vars definidas no flow se houver fonte; senao um grupo extensivel). Busca por texto. onPick(token) com token {{...}} mantido. DS v2 (tokens semanticos, sem hex). Acessivel (teclado/aria).

3) inspector/pickers/** — componentes de picker REUTILIZAVEIS (combobox pesquisavel) para os inspectors:
   AgentPicker, ChannelPicker, TagPicker, StagePicker, PipelinePicker (e o que mais fizer sentido: ConversionTypePicker, MemberPicker, MetaFlowPicker, CustomFieldPicker). Cada um: le do useFlowHelpers(), pesquisavel, value/onChange controlado, estado vazio/loading, DS v2. Esses serao consumidos por S04 (interactive), S05 (http), S06 (condition/notify/ai), S07 (triggers), S08 (scaffold). Exporte de um barrel pickers/index.ts. Mantenha a API generica e estavel (eles vao depender da sua assinatura).

NAO faca: consumo nos inspectors especificos (S04-S07) nem novos nodes (Onda 4). So a infra.

VALIDACAO (rode antes de terminar):
  pnpm --filter @hm/web typecheck   (ou pnpm typecheck)
  pnpm --filter @hm/web lint
  pnpm --filter @hm/web test

Ao final reporte: arquivos escritos, resultado da validacao, a API/props dos pickers exportados (para eu repassar a S04-S08), e qualquer seam fora do files_allowed (especialmente o mount do provider). NAO escreva .md de relatorio; responda no output.
